<%= title %>:

入款(<%= depositTimes %>笔):
<% deposits.forEach((item, index) => { %>
  <%= new Date(item.createdAt).toLocaleString('zh-CN', { hour12: false }) %>：第<%= index + 1 %>笔：<%= item.amount %>
<% }) %>
共 <%= deposits.reduce((sum, item) => sum + item.amount, 0) %> <%= unit %>


费率：<%= (feeRate).toFixed(2) %>%
汇率：<%= exchangeRate %>
